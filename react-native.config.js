// Font assets for the Addendum 2 board typography (Space Grotesk / IBM Plex
// Sans / IBM Plex Mono — same three families the PWA loads from Google Fonts).
// Android consumes copies in android/app/src/main/assets/fonts/ (checked in);
// re-run `npx react-native-asset` after adding new files here (also handles
// the iOS Info.plist side when an iOS build is next done on a Mac).
module.exports = {
  assets: ['./assets/fonts'],
};
